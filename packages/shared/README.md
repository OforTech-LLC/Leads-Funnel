# Shared Package

Shared utilities, types, and constants used across apps.

## Overview

This package contains code shared between the web and API applications.

- `src/` - Shared source code (types, utils, constants)
